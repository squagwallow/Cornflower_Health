# Thread Handoff Brief — April 6, 2026 (Afternoon #3)

> Read this if you are a new AI thread picking up this project.

## Start Here

1. Read `docs/runbook.md` — follow the AI onboarding checklist at the top
2. Read `docs/current-state.md` — ground truth on what exists
3. Read `docs/implementation-backlog.md` — what's done, what's next
4. Read `docs/dashboard-design.md` — **the approved Notion dashboard design spec** (new this session)

## What happened in this session (April 6 PM, Thread 3)

### 1. Completed Phase 4.1 — Rolling Baseline Computation
- Implemented `src/compute_baselines.py`: queries last 60 Notion rows, computes 7-day and 60-day rolling averages for HRV, RHR, deep sleep, HR dip
- Protects manually-seeded 60d baselines when fewer than 60 data rows exist
- Supports `--dry-run`, rounds to 1 decimal, skips metrics with <3 non-null values
- 31 new tests in `tests/test_compute_baselines.py` (all passing)
- Total test suite: 89 tests (29 normalize + 29 backfill + 31 baselines)
- Commits: `c36ff71` (implementation), `ffb7fa1` (doc updates)
- Task 4.2 (LLM coaching prompt) is now unblocked but deferred by user preference

### 2. Designed Notion Dashboard
- Researched Notion's current capabilities (Views API launched March 2026 — charts, linked views, dashboard widgets, tabs all now API-creatable)
- Researched Athlytic and Bevel UI patterns for health dashboard design
- Created and committed `docs/dashboard-design.md` — the approved design spec
- Commit: `a02e978`

### 3. Key design decisions made with user
- **Mobile-first single-column layout** (user views primarily on phone)
- **Bevel-style progressive disclosure**: recovery zone callout at top → detail in toggles
- **Hybrid data strategy**: scripted daily refresh for read-only sections (recovery, metrics, sleep, flags) + linked database views for interactive sections (manual entry, workout log)
- **Recovery breakdown is granular**: shows HRV component (x/60), RHR component (x/40), each modifier, hard gate status
- **Exertion/stress/energy elevated** to 2nd section; booster protocol demoted to collapsed toggle near bottom
- **Trend time windows**: 5 / 10 / 20 / 40 days (not standard 7/30/60)
- **API-first build**: everything drafted as spec → built as deployment script → one-shot push to Notion
- **Buttons are the only UI-only element** needed post-deployment; everything else is API-creatable
- **Unified Notion workspace**: all dashboard pages are children of existing Cornflower Health parent page alongside the database

### 4. Research artifacts saved (not in repo — workspace only)
- `/home/user/workspace/research-notion-capabilities.md` — detailed Notion API/feature capabilities (514 lines)
- `/home/user/workspace/research-health-app-patterns.md` — Athlytic & Bevel UI patterns (357 lines)
- These are reference material for the dashboard implementation; key findings are incorporated into `docs/dashboard-design.md`

## Current project status

| Phase | Status |
|---|---|
| Phase 0 — Pre-coding prerequisites | ✅ Complete |
| Phase 1 — Core backend pipeline | ✅ Complete (37/37 tests) |
| Phase 2 — Backfill | ✅ Complete (217 rows, Sept 2025 – April 2026) |
| Phase 3.1 — Render deployment | ✅ Complete |
| Phase 3.2 — Auth hardening | 🟡 Ready (not urgent — basic auth in place) |
| Phase 3.3 — Error alerting | 🟡 Ready |
| Phase 4.1 — Rolling baseline computation | ✅ Complete (89/89 tests) |
| Phase 4.2 — LLM coaching prompt | 🟡 Ready (unblocked, deferred by user) |
| Phase 5 — Intra-day monitoring | 🔴 Design complete, blocked on Phase 4.1 ✅ (now unblocked) |
| **Dashboard — Deploy script** | **🟡 Ready — NEXT PRIORITY** |
| **Dashboard — Daily update script** | **🔴 Blocked on deploy script** |
| **Dashboard — Deploy to Notion + test** | **🔴 Blocked on both scripts** |

## What to work on next

**Priority 1: Build `src/deploy_dashboard.py`**
- Read `docs/dashboard-design.md` for the full spec
- Script creates 5 pages under parent page `339d7cd8-531f-800b-b02d-efefaa086bf5`
- Uses Notion API: `POST /v1/pages` for pages, `POST /v1/blocks/{id}/children` for content blocks, `POST /v1/views` for linked database views and charts
- Stores created page/block IDs in `config/dashboard_ids.json` for the daily update script
- Supports `--dry-run` and `--page [name]` arguments
- Handle 2-level nesting limit with recursive append calls
- The database ID for linked views is `339d7cd8-531f-819f-85b2-c769696ea27c`
- Reference existing code patterns in `src/` for Notion API interaction

**Priority 2: Build `src/update_dashboard.py`**
- Reads `config/dashboard_ids.json` to find Daily Dashboard page and block IDs
- Queries today's Notion row + recent rows for baselines
- Computes recovery score using algorithm from `docs/coaching-layer.md`
- Updates scripted sections (recovery callout, breakdown, metrics, sleep, flags, booster, averages)
- Supports `--dry-run` and `--date YYYY-MM-DD`

**Priority 3: Deploy and test**
- Run deploy script once
- Run update script for today's date
- User tests on mobile
- Complete manual UI steps (buttons, "Show as Bar" display, conditional colors)

## Credentials

- **Notion Database ID:** `339d7cd8-531f-819f-85b2-c769696ea27c`
- **Notion Parent Page ID:** `339d7cd8-531f-800b-b02d-efefaa086bf5` (Cornflower Health)
- **Notion Token:** Rotated 2026-04-06. Set in Render env vars and local `.env`. Do not commit. Ask the user if needed.
- **Render URL:** `https://cornflower-health.onrender.com`
- **Webhook endpoint:** `https://cornflower-health.onrender.com/webhook`
- **Webhook secret:** Set in Render env var `HAE_WEBHOOK_SECRET`. Header: `X-Webhook-Secret`.
- **GitHub repo:** `squagwallow/Cornflower_Health`

## Key facts to remember

- HAE sends dict format `{"data": {"metrics": [...]}}` — NOT wrapped in an array
- The normalize function handles both dict and array formats
- Backfilled rows have `source_tags = ["backfill_json"]`, live rows have `["Apple Health"]`
- The Notion token was rotated on 2026-04-06 — old tokens from earlier threads are invalid
- Render free tier cold-starts in ~12 seconds after 15 min idle
- The user prefers Athlytic's approach to exertion tracking (captures low-level activity)
- Total test suite: 89 tests (29 backfill + 29 normalize + 31 baselines)
- **Notion Views API (March 2026)** is the key unlock — charts, linked views, dashboard widgets, tabs all API-creatable
- **Buttons are UI-only** — cannot be created via API; use synced_block workaround or manual creation
- **"Show as Bar/Ring"** on number properties is UI-only
- User wants **credit efficiency** — avoid unnecessary draft pushes; one-shot deployment
- User wants **notifications when approaching context limits** for clean handoffs
- Dashboard is **mobile-first** — single-column layout, no side-by-side columns

---

*Created: 2026-04-06 2:08 PM MDT*
